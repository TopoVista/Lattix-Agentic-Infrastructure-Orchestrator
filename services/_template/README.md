# Service Template

Copy this directory to create a new Spring Boot backend service with standard conventions.

- Standard package layout: `src/main/java/com/lattix/<service>`
- Shared backend contracts under `shared/backend`
- Health, readiness, liveness, and version endpoints
- OpenAPI docs via SpringDoc
- Testcontainers integration pattern
