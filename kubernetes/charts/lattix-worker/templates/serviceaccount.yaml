apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "lattix-worker.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "lattix-worker.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/part-of: lattix
