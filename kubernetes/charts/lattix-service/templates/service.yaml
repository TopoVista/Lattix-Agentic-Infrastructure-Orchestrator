apiVersion: v1
kind: Service
metadata:
  name: {{ include "lattix-service.fullname" . }}
  labels:
    app: {{ include "lattix-service.name" . }}
    release: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "lattix-service.name" . }}
    release: {{ .Release.Name }}
