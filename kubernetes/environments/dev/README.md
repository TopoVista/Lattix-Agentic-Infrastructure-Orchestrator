# Dev Kubernetes Environment

This folder contains dev overlay conventions for the Lattix platform.

- Expose ingress through cluster LoadBalancer or port-forwarding.
- Enable service mesh for staging-like traffic policies.
- Use external secrets integration with cloud-managed secret stores.
