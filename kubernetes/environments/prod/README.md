# Prod Kubernetes Environment

This folder contains production overlay conventions for the Lattix platform.

- Enforce strict network policies and RBAC controls.
- Use managed ingress, cert-manager, and external secrets.
- Require audit logs, resource quotas, and SLO observability.
